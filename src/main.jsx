import React from "react";
import ReactDOM from "react-dom/client";
import TicketCalculator from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TicketCalculator />
  </React.StrictMode>
);