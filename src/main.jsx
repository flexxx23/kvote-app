import React from "react";
import ReactDOM from "react-dom/client";
import TicketCalculator from "./TicketCalculator.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TicketCalculator />
  </React.StrictMode>
);